import { PmforDirective } from './pmfor.directive';

describe('PmforDirective', () => {
  it('should create an instance', () => {
    const directive = new PmforDirective();
    expect(directive).toBeTruthy();
  });
});
